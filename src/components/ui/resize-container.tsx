'use client';

type Props = {
    defaultWidth: number;
    children: React.ReactNode;
};

function ResizeContainer({ children, defaultWidth }: Props) {
    return <div className=""></div>;
}

export default ResizeContainer;
